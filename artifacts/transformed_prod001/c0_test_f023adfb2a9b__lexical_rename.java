package com.nzt.box.test.runnable.contact.bodytype;

public class BodyTypeTestResult {

    public boolean shouldGetEvent_renamed, shouldApplyForces, shouldRebound;

    public BodyTypeTestResult(boolean shouldGetEvent_renamed, boolean shouldApplyForces, boolean shouldRebound) {
        this.shouldGetEvent_renamed = shouldGetEvent_renamed;
        this.shouldApplyForces = shouldApplyForces;
        this.shouldRebound = shouldRebound;
    }
}